name: Dispatch Update Submodule
on:
  push:
    branches:
      - tentacle_9.1
jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch the changes
        uses: peter-evans/repository-dispatch@v3
        with:
          repository: rakshithakamath94/test-container
          event-type: update-submodule
